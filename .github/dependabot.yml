# Dependabot configuration.
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
#
# Two ecosystems are tracked: the root npm toolchain (antora/asciidoctor/pagefind)
# and the GitHub Actions pinned in .github/workflows/. PR titles use Conventional
# Commit prefixes to satisfy the repo's semantic-commit rule. Minor + patch bumps
# are grouped into a single PR per ecosystem; majors get individual PRs for review.
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    # `include: scope` makes Dependabot append the scope itself -> chore(deps):
    # for runtime deps and chore(deps-dev): for dev deps. The scope must NOT be
    # baked into `prefix`, or it doubles (chore(deps)(deps): ...).
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      npm-minor-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      actions-minor-patch:
        update-types:
          - "minor"
          - "patch"
